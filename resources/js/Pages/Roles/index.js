import React, {useState } from 'react';
import Layout from '../../Layout';

const Roles = ({ data }) => {
    return (
        <h1> Hello </h1>
    )
}

Roles.layout = page => <Layout children={page} />
export default Roles
